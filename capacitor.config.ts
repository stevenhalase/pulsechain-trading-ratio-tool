import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.pulsechain.tradingratiotool',
  appName: 'PulseChain: Trading Ratio Tool',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
